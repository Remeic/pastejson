{
  "name": "pastejson",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "test": "bun tests/run.ts && bun tests/site.ts",
    "bench": "bun scripts/bench.ts",
    "bench:browser": "bun run build && bun scripts/bench-browser.ts",
    "preview": "vite preview"
  },
  "devDependencies": {
    "@types/node": "^24.0.0",
    "@vercel/edge": "^1.3.3",
    "puppeteer-core": "^25.9.0",
    "typescript": "7",
    "vite": "^6.3.5",
    "vite-plugin-singlefile": "^2.2.0"
  }
}
